# ==================== CoPaw 环境变量配置 ====================
# 复制此文件为 .env 并填入实际值

# ==================== CoPaw 基础配置 ====================
# 工作目录（默认：/data/copaw）
# COPAW_WORKING_DIR=/data/copaw

# 配置文件名（默认：config.json）
# COPAW_CONFIG_FILE=config.json

# 日志级别：debug, info, warning, error, critical（默认：INFO）
# COPAW_LOG_LEVEL=INFO

# 是否自动初始化（默认：true）
# COPAW_AUTO_INIT=true

# ==================== Embedding 服务配置 ====================
# Embedding API Key（用于记忆向量搜索）
EMBEDDING_API_KEY=your_embedding_api_key_here

# Embedding 服务 URL（默认：DashScope）
EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# Embedding 模型名称（默认：text-embedding-v4）
EMBEDDING_MODEL_NAME=text-embedding-v4

# 向量维度（默认：1024）
EMBEDDING_DIMENSIONS=1024

# 是否启用 BM25 全文检索（默认：true）
FTS_ENABLED=true

# ==================== 模型提供商配置 ====================
# 选择一个或多个提供商配置 API Key

# ModelScope (魔搭) - https://modelscope.cn/
# 注册后获取 API Key
MODELSCOPE_API_KEY=your_modelscope_api_key_here

# DashScope (灵积) - https://dashscope.aliyuncs.com/
# 注册后获取 API Key
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# 自定义 OpenAI 兼容接口
# 如果使用其他兼容 OpenAI API 的服务，请配置以下变量
# OPENAI_API_KEY=your_openai_api_key_here
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL_NAME=gpt-4

# ==================== 其他可选配置 ====================
# 时区设置
# TZ=Asia/Shanghai

# 代理设置（如果需要）
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1
